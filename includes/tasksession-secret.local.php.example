<?php
/**
 * Optional template only. Do not copy this by hand.
 *
 * On first request the app auto-creates tasksession-secret.local.php
 * with a random secret (if TASKSESSION_SECRET_KEY is not already in env).
 * That generated file must stay on the server — do not commit or zip it.
 */
return 'replace-with-a-long-random-secret';
